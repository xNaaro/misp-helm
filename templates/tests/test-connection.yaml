apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "misp-helm.fullname" . }}-test-connection"
  labels:
    {{- include "misp-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "misp-helm.fullname" . }}:{{ .Values.services.misp.port }}']
  restartPolicy: Never
